.page.user
  = render :partial => 'users/header', locals: { list: 'followers' }
  %section.relationships.row
    %h2 Followers
    - @users.each do |r|
      = render partial: 'users/user', locals: { user: r.followee }
