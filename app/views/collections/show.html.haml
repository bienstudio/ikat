.page.user.collection
  = render :partial => 'users/header', locals: { list: 'collections' }
  %section.products
    .row
      %h3= @current_collection.name
      %p= @current_collection.description
    = render :partial => 'products/products', locals: { products: @current_collection.products }
  %footer
    -# = link_to "Delete #{@current_collection.name}", collection_path(id: @collection.id), class: "btn btn-red", method: :delete, "data-confirm" => "Are you sure you want to delete #{@collection.name}?"
